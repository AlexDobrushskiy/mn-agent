name: Docker

on:
  push:
    # Publish `master` as Docker `latest` image.
    branches:
      - master

    # Publish `v1.2.3` tags as releases.
    tags:
      - v*

env:
  # TODO: Change variable to your image's name.
  IMAGE_NAME: server

jobs:
  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - name: Deploy to instance
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USERNAME }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ secrets.DEPLOY_PORT }}
          script: |
            echo "Hello!"
            ls -la
#            docker pull docker.pkg.github.com/alexdobrushskiy/mn-agent/server:latest
#            docker container stop `cat .mn-server-container-id`
#            docker container rm `cat .mn-server-container-id`
#            docker run -d -p 8020:8020  -e DJANGO_SUPERUSER_USERNAME=${{ secrets.DEPLOY_USERNAME }} -e DJANGO_SUPERUSER_PASSWORD=${{ secrets.DEPLOY_PASSWORD }} -e DJANGO_SUPERUSER_EMAIL=${{ secrets.DEPLOY_EMAIL }} docker.pkg.github.com/alexdobrushskiy/mn-agent/server:latest > .mn-server-container-id
